# MLRAgents is the tooling, not a research project: it declares no experiment
# lanes because it runs no experiments. A research repository declares both.

[project]
name = "mlragents"
python = "uv run python"

[scheduler]
kind = "none"

[commands]
test = "uv run pytest"
